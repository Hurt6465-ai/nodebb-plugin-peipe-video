window.addEventListener('error', function(e){try{var r=document.getElementById('peipe-video-app');if(r&&!r.querySelector('.pv-runtime-error')){var d=document.createElement('div');d.className='pv-runtime-error';d.textContent='前端错误: '+(e.message||'unknown');r.appendChild(d);}}catch(_){}});
/* Peipe /video discover page v13-full
   Fixes TikTok iframe sound bug: never rewrites iframe.src in playSlide/prepareSlide.
   Mobile first. Requires static/lib/swiper-bundle.min.js and .css. */
(function () {
  'use strict';

  if (window.__peipeVideoDiscoverV13) return;
  window.PEIPE_VIDEO_VERSION = 'v13-full-from-new-js';
  window.__peipeVideoDiscoverV13 = true;

  var CONFIG = Object.assign({
    cid: 6,
    pageSize: 12,
    imageMax: 4,
    preloadVideoAhead: 3,
    preloadScanAhead: 14,
    translateCacheMs: 3 * 24 * 60 * 60 * 1000,
    coverCacheMs: 3 * 24 * 60 * 60 * 1000,
    imageMaxSide: 1440,
    imageMinCompressBytes: 120 * 1024,
    imageQuality: 0.62,
    maxVoiceSeconds: 120,
    swiperCdnCss: '/plugins/nodebb-plugin-peipe-video/static/lib/swiper-bundle.min.css',
    swiperCdnJs: '/plugins/nodebb-plugin-peipe-video/static/lib/swiper-bundle.min.js'
  }, window.PEIPE_VIDEO_CONFIG || {});

  var TEXT = {
    loading: '发现加载中...', empty: '暂无内容', loginFirst: '请先登录', publish: '发布', publishing: '发布中...', publishOk: '发布成功', publishFail: '发布失败',
    placeholder: '写点什么，或粘贴 TikTok 链接', chooseImage: '图片', send: '发布', comments: '评论', noComments: '暂无评论',
    commentPlaceholder: '发送消息...', replyTo: '回复', translate: '翻译', translating: '翻译中...', translateFail: '翻译失败',
    translateSettings: '翻译设置', google: '谷歌', ai: 'AI', sourceLang: '源语言', targetLang: '目标语言', save: '保存',
    voiceMsg: '语音消息', micDenied: '麦克风权限未开启', recordUnsupported: '当前浏览器不支持录音', uploadImage: '上传图片', enterSomething: '请输入内容、TikTok 链接或图片'
  };

  var RE = {
    tiktokGlobal: /https?:\/\/(?:www\.)?tiktok\.com\/@[^\/\s<>'"]+\/video\/(\d+)(?:\?[^\s<>'"]*)?/ig,
    tiktokOne: /https?:\/\/(?:www\.)?tiktok\.com\/@([^\/\s<>'"]+)\/video\/(\d+)/i,
    tiktokShort: /https?:\/\/(?:vt|vm)\.tiktok\.com\/[^\s<>'"]+/ig,
    tiktokToken: /(?:https?[-:\/]+)?(?:www[.-])?tiktok[.-]com[-\/\w@.%=&?]+/ig,
    imageExt: /\.(png|jpe?g|gif|webp|avif)(?:[?#].*)?$/i,
    audioExt: /\.(m4a|mp3|wav|ogg|oga|webm|aac)(?:[?#].*)?$/i
  };

  var LANGUAGE_META = {
    auto: { flag: '🌐', label: '自动检测' }, zh: { flag: '🇨🇳', label: '中文' }, en: { flag: '🇺🇸', label: 'English' },
    my: { flag: '🇲🇲', label: 'မြန်မာ' }, mm: { flag: '🇲🇲', label: 'မြန်မာ' }, th: { flag: '🇹🇭', label: 'ไทย' },
    vi: { flag: '🇻🇳', label: 'Tiếng Việt' }, km: { flag: '🇰🇭', label: 'ភាសាខ្មែរ' }, lo: { flag: '🇱🇦', label: 'ລາວ' },
    ja: { flag: '🇯🇵', label: '日本語' }, ko: { flag: '🇰🇷', label: '한국어' }, ms: { flag: '🇲🇾', label: 'Malay' },
    id: { flag: '🇮🇩', label: 'Indonesia' }, tl: { flag: '🇵🇭', label: 'Tagalog' }, fr: { flag: '🇫🇷', label: 'Français' },
    de: { flag: '🇩🇪', label: 'Deutsch' }, es: { flag: '🇪🇸', label: 'Español' }, ru: { flag: '🇷🇺', label: 'Русский' }
  };

  var state = {
    root: null,
    swiper: null,
    list: [],
    page: 1,
    feedLoading: false,
    feedDone: false,
    index: 0,
    players: new Map(),
    imageSwipers: new Map(),
    viewerSwiper: null,
    hasInteracted: false,
    soundUnlocked: safeJsonGet('pv-sound-unlocked', false),
    lastTap: { time: 0, x: 0, y: 0, timer: 0 },
    comments: { item: null, posts: [], replyTo: null, voiceBlob: null, voiceUrl: '', voiceDuration: 0, mediaRecorder: null, stream: null, chunks: [], startAt: 0, timer: 0, dragging: false, dragStartY: 0 },
    compose: { imageFiles: [], imageUrls: [] },
    translateTimer: 0
  };

  function $(sel, root) { return (root || document).querySelector(sel); }
  function $$(sel, root) { return Array.prototype.slice.call((root || document).querySelectorAll(sel)); }
  function norm(v) { return String(v || '').replace(/\s+/g, ' ').trim(); }
  function escapeHtml(s) { return String(s || '').replace(/[&<>'"]/g, function (c) { return ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[c]; }); }
  function rel(path) { var base = (window.config && window.config.relative_path) || ''; if (!path) return base || ''; if (/^https?:\/\//i.test(path)) return path; if (path.indexOf(base + '/') === 0) return path; return base + path; }
  function csrfToken() { return (window.config && (window.config.csrf_token || window.config.csrfToken)) || (($('meta[name="csrf-token"]') || {}).content) || ''; }
  function currentUser() { return (window.app && window.app.user) || (window.config && window.config.user) || null; }
  function isLoggedIn() { var u = currentUser(); return !!(u && Number(u.uid || 0) > 0); }
  function uidSuffix() { var u = currentUser(); return String(u && u.uid || 'guest'); }
  function alertError(msg) { if (window.app && app.alertError) app.alertError(msg); else window.alert(msg); }
  function alertSuccess(msg) { if (window.app && app.alertSuccess) app.alertSuccess(msg); }
  function safeJsonGet(key, fallback) { try { var raw = localStorage.getItem(key); return raw ? JSON.parse(raw) : fallback; } catch (e) { return fallback; } }
  function safeJsonSet(key, value) { try { localStorage.setItem(key, JSON.stringify(value)); } catch (e) {} }
  function apiFetch(url, options) { options = options || {}; options.credentials = options.credentials || 'same-origin'; options.headers = Object.assign({ accept: 'application/json', 'x-requested-with': 'XMLHttpRequest' }, options.headers || {}); return fetch(rel(url), options).then(function (res) { return res.json().catch(function () { return {}; }).then(function (json) { if (!res.ok) throw new Error(json.error || json.message || (json.status && json.status.message) || ('HTTP ' + res.status)); return json; }); }); }
  function formatCount(n) { n = Number(n || 0); if (n >= 10000) return (n / 10000).toFixed(n >= 100000 ? 1 : 2).replace(/\.0+$/, '') + '万'; return String(Math.max(0, Math.floor(n))); }
  function pad(n) { return String(n).padStart(2, '0'); }
  function formatDuration(sec) { sec = Math.max(0, Math.floor(Number(sec) || 0)); return pad(Math.floor(sec / 60)) + ':' + pad(sec % 60); }
  function parseTime(value) { if (!value) return 0; if (typeof value === 'number') return value > 9999999999 ? value : value * 1000; if (/^\d+$/.test(String(value))) { var n = Number(value); return n > 9999999999 ? n : n * 1000; } var t = Date.parse(value); return Number.isNaN(t) ? 0 : t; }
  function relativeTime(v) { var t = parseTime(v); if (!t) return ''; var d = Math.max(0, Date.now() - t), m = 60000, h = 60 * m, day = 24 * h; if (d >= day) return Math.floor(d / day) + '天前'; if (d >= h) return Math.floor(d / h) + '小时前'; if (d >= m) return Math.max(1, Math.floor(d / m)) + '分钟前'; return '刚刚'; }

  function cleanDisplayText(text) {
    var lines = String(text || '').replace(RE.tiktokGlobal, '').replace(RE.tiktokShort, '').replace(RE.tiktokToken, '').replace(/!\[[^\]]*\]\([^)]+\)/g, '').replace(/\[\s*(?:语音消息|语音动态|voice\s*message|audio\s*message)[^\]]*\]\([^)]+\)/ig, '').split(/[\r\n]+/).map(norm).filter(function (line) { return line && !/^(?:新动态|图片分享|图片动态|语音消息|语音动态|image|photo|picture)(?:\s*:?\s*\d{1,2}:\d{2})?$/i.test(line); });
    return lines.join('\n');
  }
  function displayText(item) { return cleanDisplayText(item && (item.text || item.title || '')); }
  function authorHref(author) { return author && author.userslug ? rel('/user/' + encodeURIComponent(author.userslug)) : '#'; }
  function isOwnAuthor(author) { var u = currentUser(); return !!(u && author && String(u.uid) === String(author.uid)); }

  function loadAsset(tag, url) { return new Promise(function (resolve, reject) { if (tag === 'script' && window.Swiper) return resolve(); var existing = document.querySelector(tag + '[data-pv-swiper]'); if (existing) { if (tag === 'link') return resolve(); existing.addEventListener('load', resolve, { once: true }); existing.addEventListener('error', reject, { once: true }); return; } var el = document.createElement(tag); el.dataset.pvSwiper = '1'; if (tag === 'link') { el.rel = 'stylesheet'; el.href = rel(url); document.head.appendChild(el); return resolve(); } el.src = rel(url); el.async = true; el.onload = resolve; el.onerror = reject; document.head.appendChild(el); }); }
  function ensureSwiper() { if (window.Swiper) return Promise.resolve(true); return loadAsset('link', CONFIG.swiperCdnCss).then(function () { return loadAsset('script', CONFIG.swiperCdnJs); }).then(function () { return !!window.Swiper; }); }
  function addResourceHints() { ['https://www.tiktok.com', 'https://www.tiktokcdn.com'].forEach(function (href) { if (document.querySelector('link[rel="preconnect"][href="' + href + '"]')) return; var p = document.createElement('link'); p.rel = 'preconnect'; p.href = href; p.crossOrigin = 'anonymous'; document.head.appendChild(p); var d = document.createElement('link'); d.rel = 'dns-prefetch'; d.href = href; document.head.appendChild(d); }); }

  function loadFeed(refresh) {
    if (state.feedLoading || (state.feedDone && !refresh)) return Promise.resolve();
    state.feedLoading = true;
    if (refresh) { state.page = 1; state.feedDone = false; state.list = []; state.index = 0; state.players.clear(); }
    return apiFetch('/api/v3/plugins/peipe-video/feed?page=' + state.page + '&pageSize=' + CONFIG.pageSize).then(function (json) {
      var payload = json.response || json;
      var items = (payload.items || []).map(function (item) { item.images = (item.images || []).slice(0, CONFIG.imageMax); item.viewer = item.viewer || {}; item.counts = item.counts || {}; item.text = displayText(item); return item; }).filter(function (item) { return (item.tiktoks && item.tiktoks[0]) || (item.images && item.images.length); });
      state.list = refresh ? items : state.list.concat(items); state.feedDone = payload.hasMore === false || !items.length; state.page += 1; updateSwiperSlides(refresh); if (!state.list.length) showEmpty(TEXT.empty);
    }).catch(function (err) { console.error('[peipe-video] feed failed', err); showEmpty(err.message || TEXT.empty); }).finally(function () { state.feedLoading = false; });
  }

  function initSwiper() {
    var el = $('.pv-swiper', state.root); if (!el || !window.Swiper || state.swiper) return;
    state.swiper = new window.Swiper(el, {
      direction: 'vertical', slidesPerView: 1, speed: 260, threshold: 2, resistanceRatio: 0.36, longSwipesRatio: 0.12, followFinger: true, watchSlidesProgress: true,
      preventClicks: false, preventClicksPropagation: false, passiveListeners: false, touchStartPreventDefault: false, touchMoveStopPropagation: false,
      virtual: { enabled: true, addSlidesBefore: 2, addSlidesAfter: Math.max(10, CONFIG.preloadScanAhead), slides: state.list.map(renderSlideHtml) },
      on: {
        init: function (swiper) { state.index = swiper.activeIndex || 0; afterVirtualUpdate(); activateCurrent(false); showComposeFabInitial(); },
        slideChangeTransitionStart: function (swiper) { hideComposerFab(); pauseSlide(swiper.previousIndex); state.hasInteracted = true; },
        slideChange: function (swiper) { state.index = swiper.activeIndex || 0; if (state.index >= state.list.length - 4) loadFeed(false); afterVirtualUpdate(); activateCurrent(false); },
        virtualUpdate: afterVirtualUpdate,
        reachEnd: function () { loadFeed(false); }
      }
    });
  }
  function updateSwiperSlides(reset) { if (!state.swiper) { initSwiper(); return; } state.swiper.virtual.slides = state.list.map(renderSlideHtml); state.swiper.virtual.update(true); if (reset) state.swiper.slideTo(0, 0); afterVirtualUpdate(); }
  function afterVirtualUpdate() { if (!state.root) return; $$('.pv-slide-item', state.root).forEach(function (slide) { var i = Number(slide.dataset.index || -1); slide.classList.toggle('is-active', i === state.index); prepareSlide(i, false); }); initImageSwipers(); preloadNextVideos(state.index); }

  function renderSlideHtml(item, index) {
    item = item || {}; var text = displayText(item); var hasVideo = !!(item.tiktoks && item.tiktoks[0]); var images = (item.images || []).slice(0, CONFIG.imageMax); var author = item.author || {}; var avatar = author.picture ? '<img class="pv-avatar" src="' + escapeHtml(author.picture) + '" alt="avatar">' : '<div class="pv-avatar"></div>';
    var liked = !!(readVote(item.pid, item.tid) || item.viewer.liked); var following = !!(readFollow(author) || item.viewer.following);
    var media = hasVideo ? '<div class="pv-video-shell" data-video-id="' + escapeHtml(item.tiktoks[0].videoId) + '"></div>' : renderImageMain(item);
    return '<section class="pv-slide-item ' + (hasVideo ? 'is-video' : 'is-image') + '" data-index="' + index + '">' +
      '<div class="pv-media">' + media + '</div>' + (hasVideo ? '<div class="pv-cover"><img alt="cover"></div>' : '') + '<div class="pv-gradient"></div>' + (hasVideo ? '<div class="pv-tap-zone" data-index="' + index + '"></div>' : '') +
      '<div class="pv-toolbar"><div class="pv-avatar-wrap"><a href="' + authorHref(author) + '">' + avatar + '</a>' + (author.uid && !isOwnAuthor(author) ? '<button class="pv-follow-plus ' + (following ? 'is-following' : '') + '" data-index="' + index + '">' + (following ? '✓' : '+') + '</button>' : '') + '</div>' +
      '<button class="pv-action pv-like ' + (liked ? 'is-active' : '') + '" data-index="' + index + '"><span class="pv-action-icon">' + iconHeart() + '</span><span>' + formatCount(item.counts.likes) + '</span></button>' +
      '<button class="pv-action pv-comment-btn" data-index="' + index + '"><span class="pv-action-icon">' + iconComment() + '</span><span>' + formatCount(item.counts.comments) + '</span></button>' + (images.length && hasVideo ? '<button class="pv-action pv-album-btn" data-index="' + index + '"><span class="pv-action-icon">' + iconPhoto() + '</span><span>' + images.length + '</span></button>' : '') + '</div>' +
      '<div class="pv-desc"><a class="pv-username" href="' + authorHref(author) + '">@' + escapeHtml(author.username || author.userslug || '用户') + '</a>' + (text ? '<div class="pv-text-row" data-index="' + index + '"><span class="pv-text-main">' + escapeHtml(text) + '</span> <button class="pv-translate-btn" data-index="' + index + '" aria-label="' + TEXT.translate + '">' + iconTranslate() + '</button></div><div class="pv-translated"></div>' : '') + '<div class="pv-time">' + relativeTime(item.createdAt) + '</div></div></section>';
  }
  function iconHeart() { return '<svg class="pv-heart-svg" viewBox="0 0 48 48" aria-hidden="true"><path d="M24 41s-2.2-1.3-5.2-3.4C10.2 31.4 5 25.8 5 18.6 5 12.7 9.5 8 15.2 8c3.5 0 6.6 1.8 8.8 4.7C26.2 9.8 29.3 8 32.8 8 38.5 8 43 12.7 43 18.6c0 7.2-5.2 12.8-13.8 19C26.2 39.7 24 41 24 41z"></path></svg>'; }
  function iconComment() { return '<svg class="pv-comment-svg" viewBox="0 0 48 48" aria-hidden="true"><path d="M24 8C14.6 8 7 14.7 7 23c0 5.1 2.9 9.7 7.3 12.4l-.8 5 5.9-3.2c1.2.2 2.4.3 3.6.3 9.4 0 17-6.7 17-15S33.4 8 24 8z"></path><circle cx="17" cy="23" r="2.2"></circle><circle cx="24" cy="23" r="2.2"></circle><circle cx="31" cy="23" r="2.2"></circle></svg>'; }
  function iconPhoto() { return '<svg viewBox="0 0 48 48"><path d="M10 10h28a4 4 0 0 1 4 4v20a4 4 0 0 1-4 4H10a4 4 0 0 1-4-4V14a4 4 0 0 1 4-4zm5 23h18l-6-8-4 5-3-4-5 7zm2-13a4 4 0 1 0 0-8 4 4 0 0 0 0 8z"></path></svg>'; }
  function iconTranslate() { return '<i class="fa-solid fa-language" aria-hidden="true"></i>'; }
  function iconMic() { return '<svg class="pv-mic-svg" viewBox="0 0 24 24"><path d="M12 14a3 3 0 0 0 3-3V6a3 3 0 0 0-6 0v5a3 3 0 0 0 3 3z"></path><path d="M5 11a7 7 0 0 0 14 0M12 18v3m-4 0h8"></path></svg>'; }
  function iconSend() { return '<svg class="pv-send-svg" viewBox="0 0 24 24"><path d="M4 12h14M13 6l6 6-6 6"></path></svg>'; }

  function renderImageMain(item) { var images = (item.images || []).slice(0, CONFIG.imageMax); var index = state.list.indexOf(item); return '<div class="pv-image-main"><div class="pv-image-swiper swiper" data-index="' + index + '"><div class="swiper-wrapper">' + images.map(function (src) { return '<div class="swiper-slide pv-image-swiper-slide"><img src="' + escapeHtml(src) + '" alt="image"></div>'; }).join('') + '</div>' + (images.length > 1 ? '<div class="pv-image-pagination"></div>' : '') + '</div></div>'; }
  function initImageSwipers() { if (!window.Swiper) return; $$('.pv-image-swiper', state.root).forEach(function (el) { var idx = Number(el.dataset.index || -1); if (state.imageSwipers.has(idx) && state.imageSwipers.get(idx).el === el) return; var swiper = new window.Swiper(el, { direction: 'horizontal', nested: true, slidesPerView: 1, speed: 220, threshold: 3, pagination: { el: $('.pv-image-pagination', el), clickable: false } }); state.imageSwipers.set(idx, swiper); }); }

  function buildPlayerUrl(videoId) {
    var params = new URLSearchParams({ autoplay: '0', muted: state.soundUnlocked ? '0' : '1', loop: '1', rel: '0', controls: '1', progress_bar: '1', play_button: '1', volume_control: '1', fullscreen_button: '0', timestamp: '0', music_info: '0', description: '0', native_context_menu: '0', closed_caption: '0', playsinline: '1' });
    return 'https://www.tiktok.com/player/v1/' + encodeURIComponent(videoId) + '?' + params.toString();
  }
  function playerKey(index, videoId) { return index + ':' + videoId; }
  function ensureTikTokPlayer(index) {
    var item = state.list[index], slide = findSlide(index); if (!slide || !item || !(item.tiktoks && item.tiktoks[0])) return null;
    var tk = item.tiktoks[0], key = playerKey(index, tk.videoId), player = state.players.get(key); if (player && player.iframe && player.iframe.parentNode) return player;
    var shell = $('.pv-video-shell', slide); if (!shell) return null; shell.innerHTML = ''; var iframe = document.createElement('iframe'); iframe.className = 'pv-tiktok-frame'; iframe.src = buildPlayerUrl(tk.videoId); iframe.allow = 'autoplay; fullscreen; encrypted-media; picture-in-picture'; iframe.loading = 'eager'; iframe.fetchPriority = index === state.index ? 'high' : 'low'; iframe.referrerPolicy = 'strict-origin-when-cross-origin'; iframe.title = 'TikTok Player'; shell.appendChild(iframe);
    player = { key: key, index: index, item: item, videoId: tk.videoId, iframe: iframe, wantPlay: false, ready: false, status: 'paused' }; state.players.set(key, player); fetchCover(tk.videoId, tk.url).then(function (url) { var img = $('.pv-cover img', slide); if (url && img && !img.src) img.src = url; }); return player;
  }
  function prepareSlide(index) { if (index < 0 || index >= state.list.length) return; var item = state.list[index]; if (!item || !(item.tiktoks && item.tiktoks[0])) return; ensureTikTokPlayer(index); }
  function preloadNextVideos(fromIndex) { var found = 0, max = Math.min(state.list.length - 1, fromIndex + CONFIG.preloadScanAhead); for (var i = Math.max(0, fromIndex); i <= max && found < CONFIG.preloadVideoAhead; i += 1) { var item = state.list[i]; if (item && item.tiktoks && item.tiktoks[0]) { prepareSlide(i); found += 1; } } }
  function activateCurrent() { state.index = state.swiper ? state.swiper.activeIndex : state.index; $$('.pv-slide-item', state.root).forEach(function (slide) { var i = Number(slide.dataset.index || -1); slide.classList.toggle('is-active', i === state.index); if (i !== state.index) pauseSlide(i); }); preloadNextVideos(state.index); playSlide(state.index, state.hasInteracted || state.soundUnlocked); prunePlayers(); }
  function sendToPlayer(iframe, type, value) { if (!iframe || !iframe.contentWindow) return; var msg = { 'x-tiktok-player': true, type: type }; if (arguments.length >= 3) msg.value = value; iframe.contentWindow.postMessage(msg, 'https://www.tiktok.com'); }
  function playSlide(index, userGesture) {
    var item = state.list[index]; if (!item || !(item.tiktoks && item.tiktoks[0])) { preloadNextVideos(index); return; }
    var player = ensureTikTokPlayer(index); if (!player || !player.iframe) return; player.wantPlay = true; pauseOtherPlayers(player.key); var slide = findSlide(index); if (slide) slide.classList.add('is-loading');
    if (userGesture) { state.hasInteracted = true; state.soundUnlocked = true; safeJsonSet('pv-sound-unlocked', true); }
    sendToPlayer(player.iframe, 'play'); sendToPlayer(player.iframe, 'unMute'); setTimeout(function () { sendToPlayer(player.iframe, 'play'); sendToPlayer(player.iframe, 'unMute'); }, 80); setTimeout(function () { sendToPlayer(player.iframe, 'unMute'); sendToPlayer(player.iframe, 'play'); }, 260); setTimeout(function () { if (player.wantPlay && player.status !== 'playing') markSlidePlaying(index); }, 1800);
  }
  function pauseSlide(index) { state.players.forEach(function (p) { if (p.index !== index) return; p.wantPlay = false; p.status = 'paused'; if (p.iframe) { sendToPlayer(p.iframe, 'pause'); setTimeout(function () { sendToPlayer(p.iframe, 'pause'); }, 80); } }); var slide = findSlide(index); if (slide) slide.classList.remove('is-playing', 'is-loading'); }
  function pauseOtherPlayers(currentKey) { state.players.forEach(function (p, key) { if (key === currentKey) return; p.wantPlay = false; p.status = 'paused'; if (p.iframe) { sendToPlayer(p.iframe, 'pause'); setTimeout(function () { sendToPlayer(p.iframe, 'pause'); }, 80); if (p.index < state.index) setTimeout(function () { if (p.index !== state.index && p.iframe) p.iframe.src = 'about:blank'; }, 180); } var slide = findSlide(p.index); if (slide) slide.classList.remove('is-playing', 'is-loading'); }); }
  function prunePlayers() { state.players.forEach(function (p, key) { if (Math.abs(p.index - state.index) <= CONFIG.preloadScanAhead) return; try { if (p.iframe) { sendToPlayer(p.iframe, 'pause'); p.iframe.remove(); } } catch (e) {} state.players.delete(key); }); }
  function markSlidePlaying(index) { var slide = findSlide(index); if (!slide) return; var cover = $('.pv-cover', slide); if (cover) cover.classList.add('is-hidden'); slide.classList.add('is-playing'); slide.classList.remove('is-loading'); }
  function findSlide(index) { return $('.pv-slide-item[data-index="' + index + '"]', state.root); }
  window.addEventListener('message', function (event) { var data = event.data; if (typeof data === 'string') { try { data = JSON.parse(data); } catch (e) { return; } } if (!data || !data['x-tiktok-player']) return; var host = ''; try { host = new URL(event.origin).hostname; } catch (e) {} if (!host || !/(^|\.)tiktok\.com$|(^|\.)tiktokcdn\.com$/.test(host)) return; state.players.forEach(function (p) { if (!p.iframe || p.iframe.contentWindow !== event.source) return; if (data.type === 'onPlayerReady') { p.ready = true; if (p.wantPlay) { sendToPlayer(p.iframe, 'play'); sendToPlayer(p.iframe, 'unMute'); } return; } if (data.type === 'onStateChange') { var v = Number(data.value), w = String(data.value || '').toLowerCase(); if (v === 1 || w === 'playing') { p.status = 'playing'; state.soundUnlocked = true; safeJsonSet('pv-sound-unlocked', true); pauseOtherPlayers(p.key); markSlidePlaying(p.index); } else if (v === 2 || v === 0 || w === 'paused' || w === 'ended') { p.status = 'paused'; } } }); });
  function fetchCover(videoId, url) { var key = 'pv-cover:' + videoId, cached = safeJsonGet(key, null); if (cached && cached.url && cached.expiresAt > Date.now()) return Promise.resolve(cached.url); return fetch('https://www.tiktok.com/oembed?url=' + encodeURIComponent(url || ''), { cache: 'force-cache' }).then(function (r) { return r.ok ? r.json() : {}; }).then(function (json) { var u = json.thumbnail_url || ''; if (u) safeJsonSet(key, { url: u, expiresAt: Date.now() + CONFIG.coverCacheMs }); return u; }).catch(function () { return ''; }); }

  function unlockSoundFromGesture() { if (state.hasInteracted && state.soundUnlocked) return; state.hasInteracted = true; state.soundUnlocked = true; safeJsonSet('pv-sound-unlocked', true); playSlide(state.index, true); }
  function handleTap(e, index) { var item = state.list[index]; if (!item) return; var now = Date.now(), last = state.lastTap, dist = Math.hypot(e.clientX - last.x, e.clientY - last.y); if (last.time && now - last.time < 280 && dist < 44) { clearTimeout(last.timer); last.time = 0; toggleLike(item, findSlide(index), { x: e.clientX, y: e.clientY }); return; } last.time = now; last.x = e.clientX; last.y = e.clientY; clearTimeout(last.timer); last.timer = setTimeout(function () { var p = Array.from(state.players.values()).find(function (x) { return x.index === index; }); if (p && p.status === 'playing') pauseSlide(index); else playSlide(index, true); }, 300); }
  function showHeart(x, y) { var h = document.createElement('div'); h.className = 'pv-heart-burst'; h.textContent = '♥'; h.style.left = x + 'px'; h.style.top = y + 'px'; state.root.appendChild(h); setTimeout(function () { h.remove(); }, 720); }
  function voteKey() { return 'pv-vote-state:' + uidSuffix(); }
  function voteStore() { return safeJsonGet(voteKey(), {}); }
  function readVote(pid, tid) { var s = voteStore(); return !!(pid && s['pid:' + pid] !== undefined ? s['pid:' + pid] : tid && s['tid:' + tid]); }
  function writeVote(pid, tid, val) { var s = voteStore(); if (pid) s['pid:' + pid] = !!val; if (tid) s['tid:' + tid] = !!val; safeJsonSet(voteKey(), s); }
  function toggleLike(item, slide, point) { if (!isLoggedIn()) return alertError(TEXT.loginFirst); if (!item.pid) return; var old = !!item.viewer.liked, next = !old; item.viewer.liked = next; item.counts.likes = Math.max(0, Number(item.counts.likes || 0) + (next ? 1 : -1)); writeVote(item.pid, item.tid, next); updateLikeUi(slide, item); if (next && point) showHeart(point.x, point.y); apiFetch('/api/v3/posts/' + encodeURIComponent(item.pid) + '/vote', { method: next ? 'PUT' : 'DELETE', headers: { 'content-type': 'application/json', 'x-csrf-token': csrfToken() }, body: JSON.stringify({ delta: 1 }) }).catch(function () { item.viewer.liked = old; item.counts.likes = Math.max(0, Number(item.counts.likes || 0) + (next ? -1 : 1)); writeVote(item.pid, item.tid, old); updateLikeUi(slide, item); }); }
  function updateLikeUi(slide, item) { if (!slide) return; var btn = $('.pv-like', slide); if (!btn) return; btn.classList.toggle('is-active', !!item.viewer.liked); var icon = $('.pv-action-icon', btn); if (icon) icon.innerHTML = iconHeart(); var count = $('span:last-child', btn); if (count) count.textContent = formatCount(item.counts.likes); }
  function followKey() { return 'pv-follow-state:' + uidSuffix(); }
  function followStore() { return safeJsonGet(followKey(), {}); }
  function readFollow(author) { var s = followStore(); return !!(author && author.uid && s['uid:' + author.uid]); }
  function writeFollow(author, val) { var s = followStore(); if (author && author.uid) s['uid:' + author.uid] = !!val; safeJsonSet(followKey(), s); }
  function toggleFollow(item, slide) { if (!isLoggedIn()) return alertError(TEXT.loginFirst); var author = item.author || {}; if (!author.uid) return; var old = readFollow(author), next = !old; writeFollow(author, next); updateFollowUi(slide, next); apiFetch('/api/v3/users/' + encodeURIComponent(author.uid) + '/follow', { method: next ? 'PUT' : 'DELETE', headers: { 'x-csrf-token': csrfToken() } }).catch(function () { writeFollow(author, old); updateFollowUi(slide, old); }); }
  function updateFollowUi(slide, following) { var btn = $('.pv-follow-plus', slide); if (!btn) return; btn.classList.toggle('is-following', !!following); btn.textContent = following ? '✓' : '+'; }

  function getTranslateSettings() { return Object.assign({ provider: 'google', sourceLang: 'auto', targetLang: (navigator.language || 'zh').split('-')[0] || 'zh', aiEndpoint: '', aiModel: '', aiApiKey: '', aiPrompt: '你是专业论坛翻译助手。请把用户提供的内容从 {{sourceLang}} 翻译为 {{targetLang}}。只输出译文。' }, safeJsonGet('pv-translate-settings', {})); }
  function langOptions(auto) { var keys = (auto ? ['auto'] : []).concat(['zh', 'en', 'my', 'th', 'vi', 'km', 'lo', 'ja', 'ko', 'ms', 'id', 'tl', 'fr', 'de', 'es', 'ru']); return keys.map(function (k) { var m = LANGUAGE_META[k] || { flag: '🏳️', label: k }; return '<option value="' + k + '">' + m.flag + ' ' + m.label + '</option>'; }).join(''); }
  function translateText(text) { var clean = cleanDisplayText(text).replace(/https?:\/\/\S+/g, '').trim(); if (!clean) return Promise.resolve(''); var s = getTranslateSettings(), key = 'pv-tr:' + s.provider + ':' + s.sourceLang + ':' + s.targetLang + ':' + encodeURIComponent(clean).slice(0, 240), cached = safeJsonGet(key, null); if (cached && cached.expiresAt > Date.now()) return Promise.resolve(cached.text); if (s.provider === 'ai') return translateViaAI(clean, s).then(cache); return translateViaGoogle(clean, s).then(cache); function cache(out) { out = norm(out); if (out) safeJsonSet(key, { text: out, expiresAt: Date.now() + CONFIG.translateCacheMs }); return out; } }
  function translateViaGoogle(text, s) { var url = 'https://translate.googleapis.com/translate_a/single?' + new URLSearchParams({ client: 'gtx', sl: s.sourceLang || 'auto', tl: s.targetLang || 'zh', dt: 't', q: text }).toString(); return fetch(url, { credentials: 'omit', cache: 'force-cache' }).then(function (r) { if (!r.ok) throw new Error('translate failed'); return r.json(); }).then(function (data) { return (Array.isArray(data && data[0]) ? data[0] : []).map(function (p) { return p && p[0] || ''; }).join(''); }); }
  function translateViaAI(text, s) { if (!s.aiEndpoint || !s.aiModel || !s.aiApiKey) return Promise.reject(new Error('AI translate not configured')); var endpoint = /\/(chat\/completions|responses)$/i.test(s.aiEndpoint) ? s.aiEndpoint : s.aiEndpoint.replace(/\/+$/, '') + '/chat/completions'; var prompt = String(s.aiPrompt || '').replace(/{{\s*sourceLang\s*}}/gi, s.sourceLang).replace(/{{\s*targetLang\s*}}/gi, s.targetLang); return fetch(endpoint, { method: 'POST', headers: { 'content-type': 'application/json', authorization: 'Bearer ' + s.aiApiKey }, body: JSON.stringify({ model: s.aiModel, temperature: 0.3, messages: [{ role: 'system', content: prompt }, { role: 'user', content: text }] }) }).then(function (r) { return r.json().then(function (j) { if (!r.ok) throw new Error('AI translate failed'); return j; }); }).then(function (j) { return (j.choices && j.choices[0] && j.choices[0].message && j.choices[0].message.content) || j.output_text || ''; }); }
  function translateSlide(item, slide) { var box = $('.pv-translated', slide); if (!box) return; if (box.classList.contains('is-show') && box.dataset.loaded === '1') { box.classList.remove('is-show'); return; } box.classList.add('is-show'); box.textContent = TEXT.translating; translateText(item.text || item.title).then(function (out) { box.textContent = out || ''; box.dataset.loaded = out ? '1' : '0'; if (!out) box.classList.remove('is-show'); }).catch(function () { box.textContent = TEXT.translateFail; }); }
  function openTranslateSettings() { var panel = $('.pv-translate-panel', state.root), s = getTranslateSettings(); $('[name="provider"]', panel).value = s.provider; $('[name="sourceLang"]', panel).value = s.sourceLang; $('[name="targetLang"]', panel).value = s.targetLang; $('[name="aiEndpoint"]', panel).value = s.aiEndpoint || ''; $('[name="aiModel"]', panel).value = s.aiModel || ''; $('[name="aiApiKey"]', panel).value = s.aiApiKey || ''; $('[name="aiPrompt"]', panel).value = s.aiPrompt || ''; panel.classList.toggle('is-ai', s.provider === 'ai'); $$('.pv-provider-tab', panel).forEach(function (t) { t.classList.toggle('is-active', t.dataset.provider === s.provider); }); panel.classList.add('is-open'); $('.pv-modal-backdrop', state.root).classList.add('is-open'); }
  function closeTranslateSettings() { $('.pv-translate-panel', state.root).classList.remove('is-open'); $('.pv-modal-backdrop', state.root).classList.remove('is-open'); }

  function voiceBars() { return [9, 14, 19, 11, 22, 16, 12, 20, 10, 17].map(function (h) { return '<i style="height:' + h + 'px"></i>'; }).join(''); }
  function parseDurationFromUrl(url) { try { var u = new URL(url, location.origin); return Number(u.searchParams.get('haa8dur') || u.searchParams.get('dur') || 0) || 0; } catch (e) { return 0; } }
  function renderVoiceCard(audio, cls) { return '<button class="pv-voice-card ' + (cls || '') + '" data-src="' + escapeHtml(audio.url) + '"><span class="pv-voice-play">▶</span><span class="pv-voice-wave">' + voiceBars() + '</span><span class="pv-voice-time">' + formatDuration(audio.duration || parseDurationFromUrl(audio.url)) + '</span></button>'; }
  function toggleVoiceCard(card) { var src = card && card.dataset.src; if (!src) return; var audio = card._pvAudio || new Audio(src); card._pvAudio = audio; $$('.pv-voice-card.playing', state.root).forEach(function (n) { if (n !== card && n._pvAudio) { n._pvAudio.pause(); n.classList.remove('playing'); $('.pv-voice-play', n).textContent = '▶'; } }); audio.onended = function () { card.classList.remove('playing'); $('.pv-voice-play', card).textContent = '▶'; audio.currentTime = 0; }; if (audio.paused) { audio.play().then(function () { card.classList.add('playing'); $('.pv-voice-play', card).textContent = '❚❚'; }).catch(function () {}); } else { audio.pause(); card.classList.remove('playing'); $('.pv-voice-play', card).textContent = '▶'; } }
  function isAudioHref(h) { return RE.audioExt.test(String(h || '').split('?')[0]) || /[?&](?:haa8dur|dur|duration)=/i.test(String(h || '')); }
  function extractAudiosFromContent(content) { var out = [], raw = String(content || ''); raw.replace(/<a[^>]+href=["']([^"']+)["'][^>]*>([\s\S]*?)<\/a>/ig, function (m, href, label) { if (isAudioHref(href) && !out.some(function (a) { return a.url === href; })) out.push({ url: href, label: norm(label.replace(/<[^>]*>/g, '')) || TEXT.voiceMsg }); return m; }); raw.replace(/\[([^\]]*)\]\(([^)]+)\)/g, function (m, label, href) { if (isAudioHref(href) && !out.some(function (a) { return a.url === href; })) out.push({ url: href, label: norm(label) || TEXT.voiceMsg }); return m; }); return out; }
  function openComments(item) { state.comments.item = item; state.comments.replyTo = null; var panel = $('.pv-comments-panel', state.root); $('.pv-drawer-backdrop', state.root).classList.add('is-open'); panel.classList.add('is-open'); $('.pv-comments-title', panel).textContent = TEXT.comments + ' ' + formatCount(item.counts.comments); $('.pv-comments-list', panel).innerHTML = '<div class="pv-meta">加载中...</div>'; apiFetch('/api/topic/' + encodeURIComponent(item.tid)).then(function (json) { renderComments((json.posts || []).slice(1)); }).catch(function () { $('.pv-comments-list', panel).innerHTML = '<div class="pv-meta">加载失败</div>'; }); updateCommentActionButton(); }
  function closeComments() { stopCommentRecording(); setCommentVoice(null, 0); $('.pv-comments-panel', state.root).classList.remove('is-open'); $('.pv-drawer-backdrop', state.root).classList.remove('is-open'); }
  function renderComments(posts) { var list = $('.pv-comments-list', state.root); if (!posts.length) { list.innerHTML = '<div class="pv-meta pv-empty-comments">' + TEXT.noComments + '</div>'; return; } list.innerHTML = posts.map(function (post) { var user = post.user || {}, div = document.createElement('div'); div.innerHTML = post.content || post.raw || ''; var text = cleanDisplayText(div.textContent || post.content || ''); var audios = extractAudiosFromContent(post.content || post.raw || ''); return '<div class="pv-comment" data-pid="' + escapeHtml(post.pid || '') + '" data-username="' + escapeHtml(user.username || '用户') + '"><img src="' + escapeHtml(user.picture || '') + '" alt="avatar"><div class="pv-comment-body"><div class="pv-comment-name">' + escapeHtml(user.username || '用户') + '</div>' + (text ? '<div class="pv-comment-text">' + escapeHtml(text) + '</div>' : '') + audios.map(function (a) { return renderVoiceCard(a); }).join('') + '<div class="pv-comment-translated"></div><div class="pv-comment-actions"><button class="pv-comment-reply">' + TEXT.replyTo + '</button><button class="pv-comment-translate">' + iconTranslate() + '</button></div></div></div>'; }).join(''); }
  function setReplyTarget(el) { state.comments.replyTo = { username: el.dataset.username || '用户', pid: el.dataset.pid || '' }; $('.pv-reply-bar', state.root).classList.add('is-open'); $('.pv-reply-name', state.root).textContent = '@' + state.comments.replyTo.username; $('.pv-comment-input', state.root).focus(); }
  function clearReplyTarget() { state.comments.replyTo = null; $('.pv-reply-bar', state.root).classList.remove('is-open'); }
  function recorderMime() { if (!window.MediaRecorder) return ''; return ['audio/webm;codecs=opus', 'audio/webm', 'audio/ogg;codecs=opus', 'audio/ogg'].find(function (t) { return MediaRecorder.isTypeSupported && MediaRecorder.isTypeSupported(t); }) || ''; }
  function toggleCommentRecording() { var btn = $('.pv-comment-action-btn', state.root); if (state.comments.mediaRecorder && state.comments.mediaRecorder.state === 'recording') { stopCommentRecording(); return; } if (!navigator.mediaDevices || !navigator.mediaDevices.getUserMedia || !window.MediaRecorder) return alertError(TEXT.recordUnsupported); navigator.mediaDevices.getUserMedia({ audio: true }).then(function (stream) { state.comments.stream = stream; state.comments.chunks = []; state.comments.startAt = Date.now(); var mime = recorderMime(); var opt = mime ? { mimeType: mime, audioBitsPerSecond: 16000 } : { audioBitsPerSecond: 16000 }; var rec = new MediaRecorder(stream, opt); state.comments.mediaRecorder = rec; rec.ondataavailable = function (e) { if (e.data && e.data.size) state.comments.chunks.push(e.data); }; rec.onstop = function () { var dur = Math.max(1, Math.round((Date.now() - state.comments.startAt) / 1000)); if (state.comments.stream) state.comments.stream.getTracks().forEach(function (t) { t.stop(); }); clearInterval(state.comments.timer); $('.pv-comment-record-panel', state.root).classList.remove('is-open'); if (state.comments.chunks.length) setCommentVoice(new Blob(state.comments.chunks, { type: state.comments.chunks[0].type || mime || 'audio/webm' }), dur); updateCommentActionButton(); }; rec.start(250); btn.classList.add('recording'); $('.pv-comment-record-panel', state.root).classList.add('is-open'); state.comments.timer = setInterval(function () { var el = $('.pv-record-time', state.root); if (el) el.textContent = formatDuration((Date.now() - state.comments.startAt) / 1000); }, 250); updateCommentActionButton(); }).catch(function () { alertError(TEXT.micDenied); }); }
  function stopCommentRecording() { var rec = state.comments.mediaRecorder; if (rec && rec.state === 'recording') { try { rec.stop(); } catch (e) {} } if (state.comments.stream) state.comments.stream.getTracks().forEach(function (t) { t.stop(); }); clearInterval(state.comments.timer); var panel = $('.pv-comment-record-panel', state.root); if (panel) panel.classList.remove('is-open'); updateCommentActionButton(); }
  function setCommentVoice(blob, duration) { if (state.comments.voiceUrl) URL.revokeObjectURL(state.comments.voiceUrl); state.comments.voiceBlob = blob || null; state.comments.voiceDuration = Math.max(0, Math.round(duration || 0)); state.comments.voiceUrl = blob ? URL.createObjectURL(blob) : ''; var wrap = $('.pv-comment-voice-preview', state.root); if (wrap) { wrap.innerHTML = blob ? renderVoiceCard({ url: state.comments.voiceUrl, duration: state.comments.voiceDuration }, 'is-preview') + '<button class="pv-comment-voice-remove">×</button>' : ''; wrap.classList.toggle('is-open', !!blob); } updateCommentActionButton(); }
  function updateCommentActionButton() { var btn = $('.pv-comment-action-btn', state.root), input = $('.pv-comment-input', state.root); if (!btn) return; var has = !!norm(input && input.value) || !!state.comments.voiceBlob, rec = state.comments.mediaRecorder && state.comments.mediaRecorder.state === 'recording'; btn.classList.toggle('is-send', has); btn.classList.toggle('recording', !!rec); btn.innerHTML = has ? iconSend() : (rec ? '<span class="pv-stop-dot"></span>' : iconMic()); }
  function handleCommentAction() { var input = $('.pv-comment-input', state.root); if ((input && norm(input.value)) || state.comments.voiceBlob) submitComment(); else toggleCommentRecording(); }
  function uploadToNodeBB(file) { var form = new FormData(); form.append('files[]', file); form.append('cid', String(CONFIG.cid)); return fetch(rel('/api/post/upload'), { method: 'POST', credentials: 'same-origin', headers: { 'x-csrf-token': csrfToken(), 'x-requested-with': 'XMLHttpRequest' }, body: form }).then(function (r) { return r.json().then(function (j) { if (!r.ok) throw new Error('upload failed'); return extractUploadUrl(j); }); }); }
  function extractUploadUrl(payload) { var q = [payload], seen = new Set(); while (q.length) { var cur = q.shift(); if (!cur || seen.has(cur)) continue; if (typeof cur === 'string' && (/^(https?:)?\//i.test(cur) || /^\/assets\//i.test(cur))) return cur; if (typeof cur !== 'object') continue; seen.add(cur); if (Array.isArray(cur)) q.push.apply(q, cur); else Object.keys(cur).forEach(function (k) { q.push(cur[k]); }); } throw new Error('upload url missing'); }
  function appendDurationParam(url, sec) { return url + (String(url).indexOf('?') === -1 ? '?' : '&') + 'haa8dur=' + encodeURIComponent(Math.max(1, Math.round(sec || 1))); }
  function submitComment() { var item = state.comments.item, input = $('.pv-comment-input', state.root), text = norm(input.value); if (!item || (!text && !state.comments.voiceBlob)) return; if (!isLoggedIn()) return alertError(TEXT.loginFirst); var btn = $('.pv-comment-action-btn', state.root); if (btn) btn.disabled = true; Promise.resolve().then(function () { var content = state.comments.replyTo ? ('@' + state.comments.replyTo.username + ' ' + text) : text; if (!state.comments.voiceBlob) return content; var file = new File([state.comments.voiceBlob], 'comment-voice-' + Date.now() + '.webm', { type: state.comments.voiceBlob.type || 'audio/webm' }); return uploadToNodeBB(file).then(function (url) { return (content ? content + '\n' : '') + '[' + TEXT.voiceMsg + ' · ' + formatDuration(state.comments.voiceDuration || 1) + '](' + appendDurationParam(url, state.comments.voiceDuration || 1) + ')'; }); }).then(function (content) { var payload = { content: content }; if (state.comments.replyTo && state.comments.replyTo.pid) payload.toPid = state.comments.replyTo.pid; return apiFetch('/api/v3/topics/' + encodeURIComponent(item.tid), { method: 'POST', headers: { 'content-type': 'application/json', 'x-csrf-token': csrfToken() }, body: JSON.stringify(payload) }); }).then(function () { input.value = ''; clearReplyTarget(); setCommentVoice(null, 0); item.counts.comments = Number(item.counts.comments || 0) + 1; openComments(item); }).catch(function () { alertError('评论失败'); }).finally(function () { if (btn) btn.disabled = false; updateCommentActionButton(); }); }

  function canCanvasEncode(type) { return new Promise(function (resolve) { try { var c = document.createElement('canvas'); c.width = c.height = 1; c.toBlob(function (b) { resolve(!!b && b.type === type); }, type, 0.8); } catch (e) { resolve(false); } }); }
  function compressImageFile(file) { if (!file || !/^image\//i.test(file.type) || /gif|svg/i.test(file.type) || file.size < CONFIG.imageMinCompressBytes) return Promise.resolve(file); return new Promise(function (resolve) { var img = new Image(), url = URL.createObjectURL(file); img.onload = function () { URL.revokeObjectURL(url); var max = CONFIG.imageMaxSide, scale = Math.min(1, max / Math.max(img.naturalWidth, img.naturalHeight)); var c = document.createElement('canvas'); c.width = Math.max(1, Math.round(img.naturalWidth * scale)); c.height = Math.max(1, Math.round(img.naturalHeight * scale)); var ctx = c.getContext('2d'); if (!ctx || !c.toBlob) return resolve(file); ctx.drawImage(img, 0, 0, c.width, c.height); canCanvasEncode('image/webp').then(function (webp) { var type = webp ? 'image/webp' : 'image/jpeg'; c.toBlob(function (blob) { if (!blob || blob.size >= file.size * 0.95) return resolve(file); var name = String(file.name || 'image').replace(/\.[^.]+$/, '') + (type === 'image/webp' ? '.webp' : '.jpg'); resolve(new File([blob], name, { type: type, lastModified: Date.now() })); }, type, CONFIG.imageQuality); }); }; img.onerror = function () { URL.revokeObjectURL(url); resolve(file); }; img.src = url; }); }
  function setPendingImages(files) { state.compose.imageUrls.forEach(function (u) { URL.revokeObjectURL(u); }); state.compose.imageFiles = files || []; state.compose.imageUrls = state.compose.imageFiles.map(function (f) { return URL.createObjectURL(f); }); $('.pv-preview-images', state.root).innerHTML = state.compose.imageUrls.map(function (u) { return '<img src="' + u + '" alt="preview">'; }).join(''); }
  function openCompose() { $('.pv-drawer-backdrop', state.root).classList.add('is-open'); $('.pv-compose-panel', state.root).classList.add('is-open'); hideComposerFab(); }
  function closeCompose() { $('.pv-compose-panel', state.root).classList.remove('is-open'); $('.pv-drawer-backdrop', state.root).classList.remove('is-open'); }
  function resetCompose() { $('.pv-compose-panel textarea', state.root).value = ''; setPendingImages([]); }
  function buildTitle(text) { return norm(String(text || '').replace(RE.tiktokGlobal, '').replace(RE.tiktokShort, '')).slice(0, 80) || '动态'; }
  function sendTopic() { if (!isLoggedIn()) return alertError(TEXT.loginFirst); var panel = $('.pv-compose-panel', state.root), textarea = $('textarea', panel), text = norm(textarea.value); if (!text && !state.compose.imageFiles.length) return alertError(TEXT.enterSomething); var btn = $('.pv-compose-submit', panel), lines = []; if (text) lines.push(text); btn.disabled = true; btn.textContent = TEXT.publishing; Promise.all(state.compose.imageFiles.map(function (f, i) { return uploadToNodeBB(f).then(function (url) { lines.push('![image](' + url + ')'); }); })).then(function () { return apiFetch('/api/v3/topics', { method: 'POST', headers: { 'content-type': 'application/json', 'x-csrf-token': csrfToken() }, body: JSON.stringify({ cid: Number(CONFIG.cid), title: buildTitle(text), content: lines.join('\n\n'), tags: [] }) }); }).then(function () { alertSuccess(TEXT.publishOk); resetCompose(); closeCompose(); loadFeed(true); }).catch(function (e) { console.warn(e); alertError(TEXT.publishFail); }).finally(function () { btn.disabled = false; btn.textContent = TEXT.send; }); }

  function buildChrome() { state.root.innerHTML = '<div class="pv-page pv-page-active"><div class="pv-swiper swiper"><div class="swiper-wrapper"></div></div><button class="pv-compose-fab">+</button><div class="pv-drawer-backdrop"></div><div class="pv-modal-backdrop"></div>' +
    '<section class="pv-compose-panel"><div class="pv-panel-head"><div class="pv-panel-title">' + TEXT.publish + '</div><button class="pv-close pv-compose-close">×</button></div><textarea placeholder="' + TEXT.placeholder + '"></textarea><div class="pv-preview-images"></div><div class="pv-compose-tools"><input type="file" class="pv-image-input" accept="image/*" multiple hidden><button class="pv-tool pv-image-btn">' + TEXT.chooseImage + '</button><button class="pv-primary pv-compose-submit">' + TEXT.send + '</button></div><div class="pv-meta"></div></section>' +
    '<section class="pv-comments-panel"><div class="pv-panel-grip"></div><div class="pv-panel-head pv-comments-drag"><div class="pv-panel-title pv-comments-title">' + TEXT.comments + '</div><button class="pv-close pv-comments-close">×</button></div><div class="pv-comments-list"></div><div class="pv-reply-bar"><span>' + TEXT.replyTo + ' </span><b class="pv-reply-name"></b><button class="pv-reply-cancel">×</button></div><div class="pv-comment-voice-preview"></div><div class="pv-comment-record-panel"><div class="pv-record-pulse"></div><div class="pv-record-bars"><i></i><i></i><i></i><i></i><i></i></div><span class="pv-record-time">00:00</span><span class="pv-record-tip">正在录音，点右侧按钮停止</span></div><div class="pv-comment-send-row"><div class="pv-comment-input-wrap"><button class="pv-comment-input-translate">' + iconTranslate() + '</button><input class="pv-comment-input" placeholder="' + TEXT.commentPlaceholder + '"><button class="pv-comment-action-btn">' + iconMic() + '</button></div></div></section>' +
    '<section class="pv-translate-panel"><div class="pv-panel-head"><div class="pv-panel-title">' + TEXT.translateSettings + '</div><button class="pv-close pv-translate-close">×</button></div><div class="pv-provider-tabs"><button class="pv-provider-tab" data-provider="google">' + TEXT.google + '</button><button class="pv-provider-tab" data-provider="ai">' + TEXT.ai + '</button><input type="hidden" name="provider" value="google"></div><div class="pv-lang-row"><label><span>' + TEXT.sourceLang + '</span><select name="sourceLang">' + langOptions(true) + '</select></label><span class="pv-lang-arrow">⇄</span><label><span>' + TEXT.targetLang + '</span><select name="targetLang">' + langOptions(false) + '</select></label></div><div class="pv-ai-settings"><label>AI 接口<input name="aiEndpoint"></label><label>模型<input name="aiModel"></label><label>密钥<input name="aiApiKey" type="password"></label><label>提示词<textarea name="aiPrompt" rows="4"></textarea></label></div><div class="pv-translate-actions"><button class="pv-primary pv-translate-save">' + TEXT.save + '</button></div></section>' +
    '<div class="pv-viewer"><div class="pv-viewer-swiper swiper"><div class="swiper-wrapper"></div><div class="pv-viewer-pagination"></div></div><button class="pv-viewer-close">×</button></div></div>'; bindChrome(); }
  function bindChrome() { state.root.addEventListener('click', onRootClick, true); state.root.addEventListener('pointerdown', onRootPointerDown, true); state.root.addEventListener('pointermove', onRootPointerMove, true); state.root.addEventListener('pointerup', onRootPointerUp, true); $('.pv-compose-fab', state.root).onclick = openCompose; $('.pv-compose-close', state.root).onclick = closeCompose; $('.pv-drawer-backdrop', state.root).onclick = function () { closeCompose(); closeComments(); }; $('.pv-image-btn', state.root).onclick = function () { $('.pv-image-input', state.root).click(); }; $('.pv-image-input', state.root).onchange = function (e) { var files = Array.from(e.target.files || []).filter(function (f) { return /^image\//i.test(f.type); }).slice(0, CONFIG.imageMax); e.target.value = ''; Promise.all(files.map(compressImageFile)).then(setPendingImages); }; $('.pv-compose-submit', state.root).onclick = sendTopic; $('.pv-comments-close', state.root).onclick = closeComments; $('.pv-reply-cancel', state.root).onclick = clearReplyTarget; $('.pv-comment-action-btn', state.root).onclick = handleCommentAction; $('.pv-comment-input', state.root).addEventListener('input', updateCommentActionButton); $('.pv-comment-input', state.root).addEventListener('keydown', function (e) { if (e.key === 'Enter') submitComment(); }); $('.pv-comment-input-translate', state.root).onclick = function () { var input = $('.pv-comment-input', state.root); if (!norm(input.value)) return; translateText(input.value).then(function (out) { if (out) { input.value = out; updateCommentActionButton(); } }); }; bindLongPress($('.pv-comment-input-translate', state.root), openTranslateSettings); $('.pv-translate-close', state.root).onclick = closeTranslateSettings; $('.pv-modal-backdrop', state.root).onclick = closeTranslateSettings; $('.pv-translate-save', state.root).onclick = saveTranslateSettings; $$('.pv-provider-tab', state.root).forEach(function (t) { t.onclick = function () { var p = $('.pv-translate-panel', state.root); $('[name="provider"]', p).value = t.dataset.provider; $$('.pv-provider-tab', p).forEach(function (x) { x.classList.toggle('is-active', x === t); }); p.classList.toggle('is-ai', t.dataset.provider === 'ai'); }; }); $('.pv-viewer-close', state.root).onclick = closeViewer; document.addEventListener('visibilitychange', function () { if (document.hidden) pauseSlide(state.index); else activateCurrent(); }); }
  function bindLongPress(el, cb) { if (!el) return; var timer = 0, sx = 0, sy = 0; el.addEventListener('pointerdown', function (e) { sx = e.clientX; sy = e.clientY; clearTimeout(timer); timer = setTimeout(cb, 560); }); el.addEventListener('pointermove', function (e) { if (Math.hypot(e.clientX - sx, e.clientY - sy) > 12) clearTimeout(timer); }); ['pointerup', 'pointercancel', 'pointerleave'].forEach(function (n) { el.addEventListener(n, function () { clearTimeout(timer); }); }); }
  function saveTranslateSettings() { var p = $('.pv-translate-panel', state.root); safeJsonSet('pv-translate-settings', { provider: $('[name="provider"]', p).value, sourceLang: $('[name="sourceLang"]', p).value, targetLang: $('[name="targetLang"]', p).value, aiEndpoint: $('[name="aiEndpoint"]', p).value, aiModel: $('[name="aiModel"]', p).value, aiApiKey: $('[name="aiApiKey"]', p).value, aiPrompt: $('[name="aiPrompt"]', p).value }); closeTranslateSettings(); }
  function onRootClick(e) { var btn; if ((btn = e.target.closest('.pv-like'))) { e.preventDefault(); e.stopPropagation(); var i = Number(btn.dataset.index); toggleLike(state.list[i], findSlide(i)); return; } if ((btn = e.target.closest('.pv-comment-btn'))) { e.preventDefault(); e.stopPropagation(); openComments(state.list[Number(btn.dataset.index)]); return; } if ((btn = e.target.closest('.pv-follow-plus'))) { e.preventDefault(); e.stopPropagation(); var fi = Number(btn.dataset.index); toggleFollow(state.list[fi], findSlide(fi)); return; } if ((btn = e.target.closest('.pv-translate-btn'))) { e.preventDefault(); e.stopPropagation(); var ti = Number(btn.dataset.index); translateSlide(state.list[ti], findSlide(ti)); return; } if ((btn = e.target.closest('.pv-album-btn'))) { e.preventDefault(); e.stopPropagation(); openViewer(state.list[Number(btn.dataset.index)].images || []); return; } if ((btn = e.target.closest('.pv-comment-reply'))) { e.preventDefault(); setReplyTarget(e.target.closest('.pv-comment')); return; } if ((btn = e.target.closest('.pv-comment-translate'))) { e.preventDefault(); var comment = e.target.closest('.pv-comment'), text = $('.pv-comment-text', comment), box = $('.pv-comment-translated', comment); if (text && box) { box.classList.add('is-show'); box.textContent = TEXT.translating; translateText(text.textContent).then(function (out) { box.textContent = out; }).catch(function () { box.textContent = TEXT.translateFail; }); } return; } if ((btn = e.target.closest('.pv-voice-card'))) { e.preventDefault(); toggleVoiceCard(btn); return; } if ((btn = e.target.closest('.pv-comment-voice-remove'))) { e.preventDefault(); setCommentVoice(null, 0); return; } var row = e.target.closest('.pv-text-row'); if (row && !e.target.closest('.pv-translate-btn')) row.classList.toggle('is-expanded'); }
  function onRootPointerDown(e) { if (!e.target.closest('input,textarea,select,.pv-comments-panel,.pv-compose-panel,.pv-translate-panel,.pv-viewer')) unlockSoundFromGesture(); clearTimeout(state.translateTimer); if (e.target.closest('.pv-translate-btn,.pv-comment-translate,.pv-text-row')) state.translateTimer = setTimeout(openTranslateSettings, 650); if (e.target.closest('.pv-comments-panel') && !e.target.closest('input,button,.pv-voice-card')) { state.comments.dragging = true; state.comments.dragStartY = e.clientY; } }
  function onRootPointerMove(e) { if (!state.comments.dragging) return; var panel = $('.pv-comments-panel', state.root), dy = Math.max(0, e.clientY - state.comments.dragStartY); if (panel) panel.style.transform = 'translateY(' + dy + 'px)'; if (dy > 4 && e.cancelable) e.preventDefault(); }
  function onRootPointerUp(e) { clearTimeout(state.translateTimer); if (state.comments.dragging) { var panel = $('.pv-comments-panel', state.root), dy = Math.max(0, e.clientY - state.comments.dragStartY); state.comments.dragging = false; if (panel) panel.style.transform = ''; if (dy > 86) { closeComments(); return; } } var tap = e.target.closest('.pv-tap-zone'); if (tap) { e.preventDefault(); e.stopPropagation(); handleTap(e, Number(tap.dataset.index)); } }
  function openViewer(images) { var viewer = $('.pv-viewer', state.root), wrap = $('.pv-viewer .swiper-wrapper', viewer); wrap.innerHTML = images.slice(0, CONFIG.imageMax).map(function (src) { return '<div class="swiper-slide"><img src="' + escapeHtml(src) + '" alt="image"></div>'; }).join(''); viewer.classList.add('is-open'); if (state.viewerSwiper) { try { state.viewerSwiper.destroy(true, true); } catch (e) {} } if (window.Swiper) state.viewerSwiper = new window.Swiper($('.pv-viewer-swiper', viewer), { direction: 'horizontal', slidesPerView: 1, pagination: { el: $('.pv-viewer-pagination', viewer) } }); }
  function closeViewer() { $('.pv-viewer', state.root).classList.remove('is-open'); }
  function showComposeFabInitial() { var f = $('.pv-compose-fab', state.root); if (f) f.classList.remove('is-hidden'); }
  function hideComposerFab() { var f = $('.pv-compose-fab', state.root); if (f) f.classList.add('is-hidden'); }
  function showEmpty(text) { var d = document.createElement('div'); d.className = 'pv-empty-page'; d.textContent = text; state.root.appendChild(d); }
  function init() { state.root = document.getElementById('peipe-video-app'); if (!state.root) return; document.body.classList.add('pv-video-mode'); addResourceHints(); buildChrome(); ensureSwiper().then(function () { initSwiper(); return loadFeed(true); }).catch(function () { showEmpty('Swiper 加载失败'); }); }
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init); else init();
})();
